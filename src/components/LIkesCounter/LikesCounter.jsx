import React, {useState} from 'react';

const LikesCounter = () => {
    const [count, setCount] = useState(0)
    return (
        <div>
let Increment = count
        </div>
    );
};

export default LikesCounter;